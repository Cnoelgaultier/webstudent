<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Poudlard{% endblock %}</title>

    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Font (pour effet magique 😄) -->
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&display=swap" rel="stylesheet">

    {% block stylesheets %}
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            padding-top: 70px;
            background: url('https://i.imgur.com/VkU0FZd.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #212529;
        }

        h1, h2, h3 {
            font-family: 'Cinzel Decorative', cursive;
        }

        .navbar-brand {
            font-family: 'Cinzel Decorative', cursive;
            font-size: 1.5rem;
        }

        footer {
            margin-top: 80px;
            padding: 20px 0;
            background-color: rgba(0, 0, 0, 0.8);
            color: #f8f9fa;
            text-align: center;
        }

        .btn-top {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 100;
        }

        main.container {
            background-color: rgba(255, 255, 255, 0.85);
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0,0,0,0.2);
        }
    </style>
    {% endblock %}
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">🏰 Poudlard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="#">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Élèves</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Matières</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Professeurs</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenu principal -->
    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>&copy; {{ "now"|date("Y") }} - École de sorcellerie de Poudlard</p>
            <p><small>Projet Symfony réalisé avec magie ✨</small></p>
        </div>
    </footer>

    <!-- Bouton retour haut -->
    <a href="#" class="btn btn-primary btn-top" title="Retour en haut">
        <i class="bi bi-arrow-up"></i> ↑
    </a>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Optionnel : Bootstrap Icons (pour icône flèche ↑) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}

</body>
</html>
